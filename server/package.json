{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npx nodemon src/main.ts",
    "build": "rimraf ./build && tsc",
    "start": "node build/main",
    "build-start": "npm run build && node build/main",

    "debagerer": "node --inspect build/main",
    "clear": "rimraf ./build",
    "test2": "tsc -w"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "rimraf": "^5.0.1",
    "@types/node": "^20.6.0",
    "@types/express": "^4.17.17"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
